{
  "stats_trips_per_day": [
    {
      "$group": {
        "_id": {
          "$dateToString": {
            "format": "%Y-%m-%d",
            "date": "$at"
          }
        },
        "count": {
          "$sum": 1
        }
      }
    },
    {
      "$sort": {
        "_id": 1
      }
    }
  ],
  "platform_credits_per_day_sql_hint": "SELECT DATE(created_at) d, SUM(CASE WHEN reason='platform_fee' THEN delta ELSE 0 END) fees FROM credits_ledger GROUP BY d ORDER BY d;"
}